{
  "private": true,
  "name": "analysis",
  "version": "0.0.0",
  "description": "Monorepo for libraries that work on scientific analysis data",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build-permeability": "npm run tsc && cheminfo-build --out-name 'permeability-analysis' --entry packages/permeability-analysis/lib-esm/index.js --root PermeabilityAnalysis",
    "check-types": "tsc --noEmit",
    "dev": "npm run tsc-watch",
    "eslint": "eslint .",
    "eslint-fix": "npm run eslint -- --fix",
    "prettier": "prettier --check .",
    "prettier-write": "prettier --write .",
    "test": "npm run tsc && npm run test-only && npm run eslint && npm run prettier",
    "test-only": "jest",
    "tsc": "tsc --build",
    "tsc-clean": "tsc --build --clean",
    "tsc-watch": "tsc --build --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cheminfo/analysis.git"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cheminfo/analysis/issues"
  },
  "homepage": "https://github.com/cheminfo/analysis#readme",
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "cheminfo-build": "^1.1.11",
    "eslint": "^8.20.0",
    "eslint-config-cheminfo-typescript": "^11.0.1",
    "jest": "^28.1.3",
    "jest-matcher-deep-close-to": "^3.0.2",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4"
  }
}
